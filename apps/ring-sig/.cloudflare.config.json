{
  "_comment": "Reference only — Cloudflare Pages is configured through the dashboard.",
  "project": "forgesworn-demos-ring-sig",
  "productionBranch": "main",
  "buildCommand": "corepack enable && pnpm install --frozen-lockfile && pnpm -F @forgesworn-demos/ring-sig build",
  "buildOutputDirectory": "apps/ring-sig/dist",
  "rootDirectory": "",
  "environmentVariables": { "NODE_VERSION": "20", "PNPM_VERSION": "9.15.0" },
  "customDomains": ["ring-sig.forgesworn.dev"]
}
